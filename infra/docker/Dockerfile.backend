# Node.js Backend Dockerfile
FROM node:20-alpine
WORKDIR /app

# Install dependencies
COPY package.json ./
COPY tsconfig.json ./

RUN npm install

# Copy source
COPY src ./src

# Expose port
EXPOSE 4000

# Run in dev mode with ts-node-dev
CMD ["npm", "run", "dev"]
